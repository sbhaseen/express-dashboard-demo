<h1>Delete Category: {{category.name}}</h1>

{{#if category_processes.length}}
  <p><strong>Delete the following processes before attempting to delete this category.</strong></p>
  <div style="margin-left: 20px; margin-top: 20px">
    <h4>Processes</h4>
    <dl>
      {{#each category_processes}}
      <dt>
        <a href={{this.url}}>{{this.name}}</a>
        <dd>{{this.summary}}</dd>
      </dt>
      {{/each}}
    </dl>
{{else}}
  <p>Do you really want to delete this Category?</p>
  <form method="POST" action="">
    <div class="form-group">
      <input id="id" class="form-control" type="hidden" name="id" required="true" value={{category._id}}>
    </div>
    <button class="btn btn-danger" type="submit">Delete</button>
  </form>
  </div>
{{/if}}